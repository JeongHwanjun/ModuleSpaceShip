using System.IO;
using UnityEngine;

public static class XMLPathUtilities
{
    public static string DefPath = Path.Combine(Directory.GetCurrentDirectory(), "Defs");
    public static string blueprintPath = Path.Combine(Directory.GetCurrentDirectory(), "Blueprints");
}
